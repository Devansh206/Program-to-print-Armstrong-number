#include<stdio.h>
int main()
{
  int n,temp,remin,armm=0
    printf("enter the number");
    scanf("%d",n);
    temp=n;
    while(n>0)
    {
      remin=n%10;
      armm=armm+(remin*remin*remin);
      n=n\10;
      }
     if(temp==arm)
       printf("%d no. is armstrong",armm);
     else
       printf("%d no. is not a armstron",armm);
     return o;
 }
      
